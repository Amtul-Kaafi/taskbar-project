# Copy to .env and adjust. Compose reads .env automatically if it exists.

# Host port for direct access to the app container
APP_PORT=3000

# Host port for the nginx reverse proxy
PROXY_PORT=8080

# Shown in the browser tab and reported by GET /api/config
TASKBAR_TITLE=Taskbar
